/**
 * Prayer surfaces are paused while GospelChannel focuses on church discovery.
 * Keep the implementation and existing data intact so the feature can be
 * restored without a migration.
 */
export const PRAYER_FEATURE_ENABLED = false;
