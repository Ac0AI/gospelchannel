# Implementation Plans

Batch 1 generated by the improve skill on 2026-06-12 (audit of commit `3e4c0127`), all DONE and deployed. Batch 2 generated 2026-07-07 (audit of commit `7508bb95` on branch `geo-seo-decision-engine`, uncommitted proof-route work included). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Context for humans (batch 2): plan 008 fixes a stored-XSS vector in JSON-LD plus structured-data correctness bugs; plan 009 surfaces the highest-value DB fields (Google ratings, service hours, socials, pastor, livestream) into schema for the GEO/AI-search strategy; plan 010 points the /for-churches funnel at claiming and makes the per-church preview page deliverable as an outreach pitch link.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Internationalize denomination options | P1 | S | — | DONE (commit 1779f5f9, branch advisor/improve-batch-1, reviewed+approved 2026-06-12) |
| 002  | Cron endpoints fail closed | P1 | S | — | DONE (commit fb541279; deployed 2026-07-14 version 61b20a62, CRON_SECRET was already set, verified 401 unauth on both endpoints; NOTE: local .env.local/.dev.vars CRON_SECRET values do NOT match production — rotate or resync before relying on smoke-test cron leg) |
| 003  | Next.js security patch + dep hygiene | P1 | M | — | DONE (commit 034ceb35, audit 85→31 vulns, reviewed+approved 2026-06-12) |
| 004  | PostHog waitUntil + claim_verified event | P1 | S | — | DONE (commit dddc0f75, reviewed+approved 2026-06-12) |
| 005  | Claim-flow route tests | P2 | M | 004, 006 | DONE (commit 7e1f4fd5, +19 tests, reviewed+approved 2026-06-12) |
| 006  | Small correctness fixes (vote race, upload ext, enrich save, access-code limit) | P2 | S | 004 | DONE (commit 57dc6449, reviewed+approved 2026-06-12) |
| 007  | Repo hygiene (dead code, lockfile, docs, CI typecheck) | P3 | S | — | DONE (commit 92457473, reviewed+approved 2026-06-12) |
| 008  | Harden JSON-LD output and fix structured-data correctness bugs | P1 | M | — | DONE (commit d0532f3d, merged to geo-seo-decision-engine, reviewed+approved 2026-07-07) |
| 009  | Surface ratings, service hours, socials, pastor, livestream in schema | P1 | M | 008 | DONE (commit d89ab0fb, merged, reviewed+approved 2026-07-07 — executor hardened the plan's null-coercion: Number(null)===0 guard added) |
| 010  | Point the church funnel at claiming; export preview pitch links | P2 | S | — | DONE (commit 2b465c2e, merged, reviewed+approved 2026-07-07 — data gate CLEARED same day: token backfill run with operator approval, 62,052 tokens generated, 0 errors; export now yields 31,009 rows, US 22,891) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 005 mocks `captureServerEvent`, which 004 introduces — run 004 first.
- 005's upload tests assert on behavior 006 changes (extension from MIME) — running 006 before 005 avoids a test rewrite.
- 002 has a PRODUCTION DEPLOY GATE: `CRON_SECRET` must be set via `npx wrangler secret put CRON_SECRET` BEFORE the fail-closed code deploys, or both nightly crons stop. Verified missing on 2026-06-12.
- 003 and 007 both touch lockfiles in different ways (pnpm-lock.yaml vs package-lock.json) — no conflict, but merge 003 first to avoid trivial rebases.
- 009 edits the same JSON-LD blocks 008 refactors and imports 008's `serializeJsonLd` — land 008 first.
- 008/009/010 were planned against the UNCOMMITTED working tree of `geo-seo-decision-engine` (proof routes, `ChurchProofRouteLandingPage.tsx`). If that work is reverted or reshaped, re-check the plans' drift gates before executing.
- Environment gotcha for all batch-2 plans: `pnpm typecheck`/`pnpm <script>` fail on this machine (`ERR_PNPM_UNSUPPORTED_ENGINE`, Node 26 vs pinned 22.x). Use `pnpm exec tsc --noEmit` and `pnpm exec vitest run ...`, which work. Fixing the Node version mismatch is a separate operator decision (nvm use 22 or widen engines).

## Findings considered, rejected or deferred (batch 2, 2026-07-07)

- **worker.ts buffers full HTML responses before sending** (`worker.ts:54-100`): real, but deferred — MED-risk streaming rewrite for an unmeasured TTFB win; revisit if TTFB becomes a complaint.
- **`?q=` search creates unbounded `unstable_cache` keys** (`src/lib/church.ts:1819-1823`): real but small; deferred — bundle into the next perf batch rather than its own plan.
- **Video/image sitemap namespaces** (20.3k video pages): deferred — sitemap rendering already runs close to Worker CPU limits (CHUNK_SIZE comments in `sitemap-data.ts:28-31`); needs a windowed-load design, not a quick add.
- **Localized metadata / hreflang for EU churches**: strategic content-language decision for the owner, not an executor plan. Cheap first step (`inLanguage` per church) noted as follow-up in plan 009.
- **Church-facing website-health report from `church_website_tech`**: deferred — `sales_angle` text is internal sales language; needs customer-safe content design before any public surface.
- **Wiring `church_outreach` as in-house CRM**: deferred pending operator decision (lemlist is the active channel; duplicate tracking would add drag now).
- **US edition of the church-tech report**: cheap (parameterize the country list in `european-church-tech-report.ts`) but a content/positioning call — operator to decide.
- **Songs/artists reverse-index pages, watch-online proof route, /near geo pages, RSS/iCal feeds, public JSON API**: grounded direction options surfaced in the 2026-07-07 audit; intentionally NOT planned — owner assignment says run the live AI-search test before scaling new page types.
- **Dead engagement code (`MostMovedSection` + `video_moved_*` tables, unmounted `ChurchHeartButton`)**: real dead weight, low priority — fold into the next hygiene batch.

## Findings considered and rejected (batch 1, 2026-06-12)

- **Module-level cache in `src/lib/church.ts:86-89` as "1.5 TB egress risk"**: rejected — the cache sits ON TOP of `unstable_cache` (line 1968); Neon is protected. Per-isolate memory footprint is a minor, accepted cost.
- **SSRF in auto-enrichment fetching suggested-church websites**: by design — the feature exists to crawl the suggested site; Workers has no internal network to pivot to.
- **Race condition in follow endpoint**: rejected — the upsert is idempotent on `(church_slug,email)`; no duplication possible.
- **Suggest route enrichment chain missing error handling**: rejected — `.catch()` exists at `suggest/route.ts:111`; the real (narrower) gap is the unchecked update result inside `saveEnrichmentToSuggestion`, fixed in plan 006.
- **Splitting the 2,072-line `src/lib/church.ts` god module**: deferred, not rejected — real debt, but refactoring it before route-level test coverage exists (plan 005) is riskier than the debt itself. Revisit after 005 lands.
- **`better-auth` upgrade (11 patch versions behind)**: deferred — auth upgrades need dedicated manual verification of the magic-link claim flow; bundling it into 003 would bloat that plan's blast radius.
- **Email header injection in `email.ts`**: not confirmed by direct read; recipient values come from validated emails and the transport is an HTTP API with JSON fields. No plan.
- **Vote cookie set without HttpOnly**: by design — the client JS reads it for UI state; the IP rate limit is the actual guard.
- **`scripts/* 2.mjs` duplicate files**: not in git (local macOS artifacts only) — nothing to fix in the repo.
- **PostHog "protobufjs critical" audit finding**: dependency-chain noise, not reachable from runtime code paths; superseded by plan 003's dep reclassification + re-audit.
